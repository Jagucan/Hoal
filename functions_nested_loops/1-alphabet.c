#include "main.h"

/**
 * main - print alphabet in lowercase
 * Return. 0 every time
 */

int main(void)
{
	char J;

	for (J = 'a'; J <= 'z'; J++)
	{
		_putchar(J);
	}
		_putchar('\n');

return (0);
}
