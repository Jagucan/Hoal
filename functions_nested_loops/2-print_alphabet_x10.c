#include "main.h"

/**
 * print_alphabet - print an alphabet in lowercase
 * Return: 0 every time
 */

void print_alphabet_x10(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet < 'z'; alphabet++)
	{
		_putchar(alphabet);
	}
		_putchar('\n');

}
