#include "main.h"
#include <math.h>

/**
 * _abs - print absolute value of an integer
 * @num: is an number
 * return: absolute value of num
 */

int _abs(int num)
{
	if (num < 0)
	{
		num = num * -1;
		return (num);
	}

	else
	{
		return (num);
	}
}
