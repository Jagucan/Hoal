#include "main.h"
#include <math.h>

/**
 * _abs - compute the absolute value of an integer.
 * @num: is an number.
 * return: absolute value of an interger. 
 */

int _abs(int num)
{
	if (num < 0)
	{
		num = num * -1;
		return (num);
	}

	else
	{
		return (num);
	}
}
