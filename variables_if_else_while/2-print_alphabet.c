#include <stdio.h>

/**
 * main contain putchar for prints the alphabet
 * return 0 every time
 */

int main(void)
{
	char j;

	for (j = 'a'; j <= 'z'; j++)	
	{
		putchar(c);
		putchar('\n');
	}
return (0);
}
