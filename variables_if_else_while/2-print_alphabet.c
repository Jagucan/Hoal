#include <stdio.h>

/**
 * main - prints an alphabet in lowercase
 * Return: 0 every time
 * main contain putchar
 */

int main(void)
{
	char j;

	for (j = 'a'; j <= 'z'; j++)
	{
		putchar(j);
	}
		putchar('\n');
return (0);
}
