#include <stdio.h>

/**
 * Main - contain putchar for prints the alphabet
 * Return: 0 every time
 * main contain for and putchar
 */

int main(void)
{
	char j;

	for (j = 'a'; j <= 'z'; j++)
	{
		putchar(j);
	}
		putchar('\n');
return (0);
}
