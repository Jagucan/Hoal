#include <stdio.h>

/**
 * main contain putchar 
 * Return 0 every time
 */

 int main(void)
{

	char j;
	
	for (j = 'a'; j <= 'z'; j++)
        {
		putchar(j);
        }
	
	for (j = 'A'; j <= 'Z'; j++)
        {
		putchar(j'\n');
	}
return (0);
}
