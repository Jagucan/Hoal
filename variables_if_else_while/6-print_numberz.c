#include <stdio.h>

/**
 * main - print numbers base 10 and no use char
 * Return: 0 every time
 */

int main(void)
{
	int j;

	for (j = '0'; j <= '9'; j++)
	{
		putchar(j);
	}
		putchar('\n');
return (0);
}
