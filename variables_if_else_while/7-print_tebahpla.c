#include <stdio.h>

/**
 * main - print an alphabet in reverse
 * Return: 0 every time
 */

int main(void)
{
	int j;

	for (j = 'z'; j >= 'a'; j--)
	{
		putchar(j);
	}
		putchar('\n');

return (0);
}
